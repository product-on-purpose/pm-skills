# Demo family contract (fixture)

Stub contract document for the family-registration parity fixture. Its only job is to
exist at the path the registry declares, so the "contract present" branch is exercised.
