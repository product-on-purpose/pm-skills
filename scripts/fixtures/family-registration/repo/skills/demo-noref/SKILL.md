# Demo no-reference member (fixture)

Failing member: this directory exists and has a SKILL.md, but the body deliberately does
NOT mention the family contract path, so the contract-reference branch must FAIL here.
