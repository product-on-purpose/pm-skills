# G1 adversarial review, round 12 (2026-08-28), the final round

**Verdict: needs-attention.** Codex returned **3 findings** (2 high, 1 medium). Trend:
**5, 6, 8, 6, 6, 6, 4, 4, 5, 5, 3, 3.** HEAD reviewed `87c2994e`, scope pre-registered at `87c2994e`
as the three surfaces `51d5bec6` changed.

**This round is pre-registered as the last adversarial round of the gate** (D23 = A, ruled and
committed at `87c2994e` before the round launched). Its dispositions therefore close G1 regardless of
what it returned, with untouched-territory findings deferred under clause 2 and the resulting fix
pass verified by a finding-versus-result table rather than by a round 13.

## Two instruments, and what corroboration bought

D23 named both instruments before the round ran, so neither can be treated as a post-hoc addition.

| Instrument | Role | Result |
|---|---|---|
| Codex companion `adversarial-review` | **Instrument of record**, matching all eleven prior rounds | 3 findings, 2 high 1 medium |
| Multi-lens Claude panel (`wf_5b30a97f-1e9`) | **Corroborating** | 27 candidates, 18 survived three-angle refute-by-default verification, 13 in scope |

**All three Codex findings were independently reproduced by the panel**, from lenses that were not
told what Codex would look for and ran concurrently with it. That is the single most useful thing
this round produced: on a gate where the central question is whether one reviewer is generating its
own work, three-for-three independent convergence is strong evidence the findings are real rather
than manufactured.

The panel's structure: five lenses (design-hole, cross-doc, fidelity, sample-sweep, governance), then
three **perspective-diverse** refuters per candidate, each defaulting to refuted. Angle 1 checked
presence in the current file, angle 2 checked whether the finding was in substance a request for
engineering rigor this repo has twice declined to author (D19, D22), angle 3 checked whether the
concern was already enforced elsewhere. A finding died on two of three refutations. **Nine of 27
candidates were killed**, which is the mechanism working: the panel's raw output would have
overstated the problem by a third.

**Disclosed coverage gap.** One refuter agent of 81 failed on a schema retry cap, so one candidate
was judged on two votes rather than three. That candidate ("the `[fictional]` convention as written
is number-scoped") survived 0-of-2 refutations and is being **deferred**, not closed, so the missing
vote changes no disposition. No lens failed; all five returned.

## Findings and dispositions

Numbered R12-F1 through R12-F6. Codex origin is marked; where the panel found the same defect
independently it is noted, because that is the round's main evidentiary product.

### Closed, because they are on the surfaces `51d5bec6` changed

| Finding | Sev | Origin | Surface |
|---|---|---|---|
| **R12-F1** the new read-logging row itself conjoins two independently falsifiable claims | high | **Codex + panel** (design-hole) | the row the fix created |
| **R12-F2** the validation rationale still enumerates the pre-split row set, so the new row has no named degraded path | medium | panel (cross-doc + fidelity, found twice) | the fix's own surrounding text |
| **R12-F3** a second hygiene row is checked on prior-release evidence | medium | **Codex + panel** (governance, high) | the fix's own class-check claim |
| **R12-F4** the reset row's new completion criterion covers half the control it restores | medium | panel (governance) | the Done cell the fix authored |
| **R12-F5** the appendix Limitations geography claim is unmarked invented fact | high | **Codex + panel** (sample-sweep) | the appendix the fix was marking |
| **R12-F6** the provenance comment is a closed enumeration that omits two invented claims in the sentence it annotates | medium | panel (sample-sweep) | the sentence the fix touched |

**R12-F1 is the finding of the round, and it is the fourth consecutive round of fresh design damage
on this one table.** The row split closed R11-F1 by asserting a principle: a per-row coverage rule is
sound only if every row carries exactly one claim. The row it created to demonstrate that principle
reads "[Whether each read is recorded, **and** whether the record identifies the reader]". Those fail
independently. An audit sink can write one row per read with no principal on it; a normal-path test
asserting an audit record is emitted plus a failure-path test asserting reads are refused when the
sink is down fills both columns while an anonymous audit trail ships. **The fix violates the
principle it was authored to enforce**, which is the cleanest instance of the authored-methodology
churn this gate has been measuring since round 9.

**R12-F3 falsifies a claim `51d5bec6` made about itself.** That commit stated "a class check
confirmed no other post-tag row is prematurely checked". The check was scoped **by section** rather
than by defect shape, so section E's "conventions enforced by CI are written where humans read them",
checked with the parenthetical "in v2.32.0", survived one section below the row that was reset. The
standing source at `../checklist_doc-update-and-hygiene.md` writes that row beginning with the word
"**New**", which is the word that scopes it to the cycle in hand; the plan copy dropped it.

**R12-F5 is the fourth recurrence of the sweep-completeness class**, and the panel's account of why it
matters is sharper than the count: the unmarked geography claim sits one line below three
`[fictional]`-marked clauses, so **the local contrast actively signals that this one is grounded**.
An unmarked invented claim next to marked ones is worse than an unmarked claim standing alone.

**On R12-F5's scope basis, stated so it cannot be attacked.** The clause sits at line 160 and
`51d5bec6` edited line 156, so calling it "on the changed surface" is arguable: it is in the appendix
the fix was sweeping, but not in the sentence the fix touched. It is closed anyway on **the round-11
precedent**, where R11-F3 was pre-existing and clause-2 deferrable and was closed regardless because
it was a trivial edit protecting a gate this cycle would run within days. The same reasoning applies
exactly: a one-token marker, in a file already being edited, closing the fourth recurrence of a class
this gate has chased since round 9. Whichever basis a reader prefers, the disposition is the same.

### Deferred to v2.34.0 with rationale, under clause 2

All are untouched territory: none was introduced or touched by `51d5bec6`, and several are outside
the three in-scope surfaces entirely.

| Finding | Why deferred |
|---|---|
| Retention, sampling and terminal-disposition rows each bundle multiple claims; retention also drops the evaluation-set copy question SKILL.md requires | Same defect shape as R12-F1 on rows the fix never touched. Closing them means **authoring** a new row decomposition across the table, which is precisely the fix mode this gate has measured as churning for four rounds. Carried as a scoped v2.34.0 effort |
| The opt-out row asks only about users, while the section's own rationale says captured text often belongs to someone who is not the user | Pre-existing; a genuine scope question about the template's model, not damage from this cycle |
| `SKILL.md` was not updated for the grant-removal requirement; its quality checklist enumerates six concern groups against twelve table rows; its decide-and-record list still fuses access and read logging | Outside the three in-scope surfaces. Real, and it is the same drift class as R10-F5 |
| Sibling interview-synthesis samples carry extensive unmarked invented methodology | Outside the in-scope surface. This is the library-wide audit already carried from round 6, not a release gate |
| The `[fictional]` convention as written is number-scoped, so the methodology-prose marking rule the last three rounds enforced exists in no standards document | **The most valuable deferred finding.** It explains why this class recurred four times: the rule being enforced was never written down, so each sweep re-derived its own scope. Fixing it means authoring a convention, which is v2.34.0 work |
| "eight months" is an unmarked spelled-out invented duration | Same class as R12-F5 but outside the appendix the fix touched |
| Section A's bump-count row is stamped with a verification date preceding the ruling its claim depends on | Pre-existing bookkeeping |

**Why the deferral split falls where it does.** It follows this gate's own measured lesson rather
than severity. **Fixes that mark or delete a claim converged; fixes that author methodology
churned.** Everything closed above is a marking, an enumeration repair, or a checkbox reset.
Everything deferred requires authoring new structure. Round 11 set the precedent for closing a
deferrable finding when it is cheap and protects a gate this cycle will run: R11-F3 was pre-existing
and closed anyway on exactly that reasoning.

## Judged against the pre-registered standard (`e32a117d`) and D23 (`87c2994e`)

No P0. Every finding is closed or deferred with written rationale. Clause 5 (fix-pass damage is P1
regardless of label) is engaged for R12-F1, R12-F2, R12-F3, R12-F4 and R12-F6.

**Per D23, this round does not produce a round 13.** The fix pass closing these findings is verified
by the finding-versus-result table below rather than by another adversarial round, and the gate is
then attested.

## Finding-versus-result verification table

D23's terminator, and the reason there is no round 13. Built **from the instrument of record's
finding texts, exploded item by item**, not from a re-derivation of what the findings probably meant.
That distinction is the whole point: the failure that recurred in rounds 9, 10 and 11 was never sweep
breadth, it was not re-reading the finding against the result, and three times the missed item was
sitting in a list already in hand.

Fix pass: see the commit this record ships in. Validators re-run after it: **pre-tag bundle exit 0,
all 19 enforcing validators PASS**, sample counts **213/63**, fabricated-metrics advisory at its
**338** baseline (unchanged, which also confirms that advisory is percentage-scoped and could never
have caught `eight months`).

### R12-F1, from Codex: the trace rows are still not atomic enough for per-row coverage

| # | Item the finding named | Result | Verified how |
|---|---|---|---|
| 1 | The new read-logging row contains two independently falsifiable claims: that an audit record exists, and that it identifies the reader | **Closed.** The row now opens "Two claims here, and they need two coverage entries", names both, and states that they fail independently, with the anonymous-audit-trail case spelled out | Read the row as it now stands in `TEMPLATE.md` |
| 2 | A test asserting only record creation can appear to cover the row while access remains unattributable | **Closed structurally, not by splitting.** The coverage rule now says a row is a heading and not a promise of atomicity, and that a row carrying more than one claim gets one entry per claim. The coverage table's key column changed from "Row from the table above" to "One claim from the table above. A row that carries two claims gets two entries here" | Read the rule sentence and the table header |
| 3 | The adjacent retention row similarly combines expiry and deletion | **Deferred to v2.34.0**, named in the deferral table above. Untouched by `51d5bec6`. Partially mitigated by item 2, which now requires the QA owner to decompose any multi-claim row including this one | Deliberate. See the deferral rationale |
| 4 | Retention omits SKILL.md's requirement that deletion requests reach evaluation-set copies | **Deferred to v2.34.0**, named explicitly in the deferral table. This one is a genuine content gap rather than a decomposition gap, and item 2 does **not** mitigate it | Deliberate, and flagged as the deferral with the least mitigation |
| 5 | Sampling combines capture rate and selection method | **Deferred to v2.34.0.** Same treatment as item 3, mitigated by item 2 | Deliberate |
| 6 | Recommendation: give audit creation, reader attribution, retention expiry, deletion propagation, sampling rate and sampling selection separate claim rows and test-table entries | **Deliberately not done, and this is the round's one rejected recommendation.** Atomizing into six authored rows is the same fix mode that produced R11-F1 from D22 and R12-F1 from R11-F1. Any bracketed row text bundles claims at some resolution, so a further split moves the defect down a level rather than removing it, and D23 leaves no round 13 to catch fresh damage. The template instead stops claiming the row set is the atomic claim set. **The case against this choice, recorded:** it leaves the decomposition to a QA owner who may not do it | Reasoned, and stated here rather than in a commit body so a future reader finds it attached to the finding |

### R12-F2, from Codex: invented methodology remains unmarked outside the repaired appendix clause

Every item Codex named in one sentence, checked one at a time. This is the table that exists because
this exact class failed three rounds running.

| # | Item the finding named | Result |
|---|---|---|
| 1 | Interviews were **semi-structured** | **Marked.** "Six semi-structured [fictional] interviews" |
| 2 | Interviews were **remote** | **Marked.** "conducted remotely [fictional]" |
| 3 | The segments were **deliberately recruited** | **Marked twice**, at both assertions: "recruited from active Campaigns v1 merchants [fictional] and deliberately split [fictional]", and in Limitations, "deliberately [fictional] over-weights non-adopters" |
| 4 | **Coding preceded theme consolidation** | **Marked.** "before theme consolidation [fictional]". *This item was named by Codex and by neither panel lens; it would have been missed had the fix been built from the corroborating instrument instead of the instrument of record* |
| 5 | Line 160 invents a **US-only participant boundary** | **Marked.** "No participant operated outside the US [fictional]" |
| 6 | The same pattern in the **Workbench** sample: consent, transcription, affinity-mapping claims | **Deferred to v2.34.0.** Different file, outside the in-scope surface, and part of the library-wide audit carried since round 6 |
| 7 | The same pattern in the **Storevine Campaigns** sample: recruitment, consent, quote-editing claims | **Deferred to v2.34.0.** Same reason |
| 8 | Recommendation: add a **non-numeric methodology-provenance check** | **Deferred to v2.34.0**, and paired there with the panel's finding that the `[fictional]` convention in `README_SAMPLES.md` is written number-scoped ("every invented metric", "any specific number"). Together those are the root cause of four recurrences: the rule being enforced was never written down, so each sweep re-derived its own scope. Fixing it means authoring a convention and a validator, which is the churning fix mode |

**One item closed that Codex did not name**, found by the panel and closed on the round-11 cheap-close
precedent: `eight months` at two places, an invented spelled-out duration. Marked in the narrative
assertion and after the quote attribution. Recorded here rather than folded in silently.

### R12-F3, from Codex: a second hygiene item is checked using prior-release evidence

| # | Item the finding named | Result |
|---|---|---|
| 1 | The documentation-consistency item is checked because CONTRIBUTING gained rules **in v2.32.0** | **Confirmed and closed.** Reset to unchecked, with the prior-cycle evidence named as the reason |
| 2 | Reset the item to unchecked | **Done** |
| 3 | Audit only v2.33.0's enforcing changes | **Done.** The row now says so and names the expanded sample-count enforcement, due G2 |
| 4 | Check it with current-cycle evidence or an explicit current-cycle N/A rationale | **Encoded in the row.** It cannot be checked at G0; it is due G2 |
| 5 | Codex's own note that "the other checked quantitative rows were revalidated at 213/63 samples and eight skill bumps" | **Independently re-verified rather than taken on the reviewer's word.** `check-sample-counts.mjs` returns 213/63. The bump count was re-derived at HEAD from `git diff v2.32.0..HEAD -- 'skills/*/SKILL.md'`: eight skills, 3 major, 4 minor, 1 patch, matching the claim exactly. The section-A row's stamp said 2026-08-16, which predated ten G1 rounds of skill edits, so the stamp was stale even though the number was right; it now carries the re-verification |

**Why the section-A date stamp was closed rather than deferred.** The panel raised it separately and
it looked deferrable. It is not, and the reason is structural: R12-F3's remedy is a class check
re-scoped **by defect shape** rather than by section, and that shape is "a row satisfied by evidence
that predates what the row's claim depends on". Run honestly, that check re-finds the section-A row.
Closing R12-F3 while deferring a same-shape member surfaced by the same check would have rebuilt
R11-F3's exact pattern inside the fix that closed it.

### Panel-only findings closed

| Finding | Result |
|---|---|
| **R12-F2 (panel)** the validation rationale still enumerates the pre-split row set, so the row the commit created has no named degraded path | **Closed.** The bullet now names read logging as its own concern with its own failure mode, "an audit writer that stops recording while reads keep succeeding", and states that the list is illustrative rather than the full set, so the next added row does not silently inherit the same gap |
| **R12-F4** the reset post-tag row's completion criterion covers half the control | **Closed.** The row now names both halves the standing source owns, `Latest shipped` and the version's own entry, with the second half's addition dated and attributed |
| **R12-F6** the provenance comment is a closed enumeration that omits two invented claims in the sentence it annotates | **Closed by replacing the enumeration with a rule.** The comment now states what the prompt supplies and declares everything else in the section invented, with the test stated as "does the prompt supply it" rather than "is it on the list", and records that a closed list has now been wrong three times |

### Class check on this fix pass itself

Run because clause 5 treats fix-pass damage as P1 and there is no round 13 to catch it.

- **Every edit in this pass is a marking, an enumeration repair, a checkbox reset, or the removal of
  an over-strong claim.** None authors new methodology or new structure. That is deliberate: across
  twelve rounds, fixes that removed a claim converged and fixes that authored methodology churned,
  and the one recommendation asking for authored structure (R12-F1 item 6) was declined on that
  evidence.
- **No new numbers, rates or durations were introduced** anywhere in the pass. The fabricated-metrics
  advisory holding at exactly 338 is the artifact confirming it.
- **No skill version bump is required.** `51d5bec6` set the precedent within this gate: fix-pass edits
  to a skill's `references/` during a release cycle do not re-bump, because the cycle's bump already
  covers them. `measure-instrumentation-spec` remains at 3.0.0, and the count of eight bumps is
  unaffected, which was re-verified above.
- **Both surfaces the pass touched in `plan_v2.33.0.md` are GATE rows due after G0**, so neither
  blocks READY TO TAG, and both now block the cycle closing, which is what they are for.

## Maintainer attestation

*(the one act this record cannot supply for itself)*
