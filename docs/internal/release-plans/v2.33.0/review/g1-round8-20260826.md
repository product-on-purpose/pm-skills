# G1 adversarial review, round 8 (2026-08-26)

**Verdict: needs-attention. Do not ship on the round as returned.** 4 findings (3 high, 1 medium).
Trend: **5, 6, 8, 6, 6, 6, 4, 4.**

Completion verified genuine: zero `usage limit`, zero `Turn failed`, one `Turn completion inferred`.
HEAD reviewed `7996d00e`, thread `01a03f7b-3739-7df2-8365-3439a20b5fe0`.

## The structural finding: one block has produced six findings in five rounds

| Round | Finding against `measure-instrumentation-spec/references/TEMPLATE.md` |
|---|---|
| 4 | R4-F1 the storage sentinel cannot exercise storage-only minimization |
| 5 | R5-F1 the forced storage-failure test passes vacuously |
| 6 | R6-F2 the forced storage-failure test is not retry-safe |
| 7 | R7-F2 egress permits delayed replay; R7-F3 the recovery assertion contradicts its own setup |
| 8 | R8-F2 the egress fault test can still pass vacuously |

**Every one is the same error: a fix applied to one boundary and not mirrored to the other.** The
storage side gained a present-at-collector assertion in round 4; egress did not get its equivalent
until this finding raised it. Storage gained a recovery re-check in round 6; egress did not get one
until round 7. Storage's vacuity was closed in round 5; **egress's is still open now.**

Patching this block one asymmetry at a time has failed five consecutive times. **The remedy is a
structural decision rather than a sixth patch**, and it is carried to the maintainer rather than
chosen here.

## Findings

| Finding | Sev | Origin |
|---|---|---|
| R8-F1 Kano adequacy still ignores signal strength | high | fix-pass damage, `7996d00e` |
| R8-F2 egress fault test can still pass vacuously | high | fix-pass damage, the asymmetry above |
| R8-F3 canonical PRD example presents invented research as fact | high | **pre-existing since v2.0.0** (`a59333ff`) |
| R8-F4 the ADR fix replaced one unsupported claim with another | medium | fix-pass damage, `7996d00e` |

### R8-F1 (high) - adequacy omits the dimension the source skill names

> The adequacy note checks count, complete pairs, recruitment, representativeness, and instrument
> quality, but omits the signal-strength dimension required by `measure-survey-analysis`. A
> representative 100-response result split almost evenly across categories satisfies every named
> condition; because the Surveyed row says to classify normally, it can still be called validated
> instead of ambiguous.

**CONFIRMED against the source.** `measure-survey-analysis:108` sets its confidence label "based on
sample, methodology, **and signal strength**". The round-7 fix imported the first two and dropped
the third, so an evenly-split distribution clears every stated condition while carrying no signal.

### R8-F2 (high) - the asymmetry, sixth instance

> Every egress assertion is an absence check. Unlike the storage test, the block never proves that
> trace capture was selected, the collector was healthy, the minimizer was reached, or the injected
> fault actually fired. Disabled sampling or a broken trace pipeline therefore passes the entire
> block.

**CONFIRMED.** The egress block is three assertions and all three are absences. The storage block
carries a present-at-collector assertion precisely because round 4 established that an all-absence
test proves nothing. That reasoning was never applied to egress.

### R8-F3 (high) - the persona defect, repeated in the flagship skill

> The canonical example claims research established a 34% duplicate-task rate and more than two
> hours of weekly manual work, then builds baselines and targets from those values. The file
> contains no fictional markers or Source Notes. Because `deliver-prd` loads this as its completed
> example, it teaches agents to invent precise research evidence and use it for product commitments.

**CONFIRMED, and it is R5-F3 recurring in a different file.** The `34%` claim dates to `a59333ff`,
the v2.0.0 release prep, roughly thirteen releases back. The file carries **zero** `[fictional]`
markers across 263 lines and roughly 11 invented quantities. This is the same
`check-sample-no-fabricated-metrics` scope gap already carried to v2.34.0: the validator globs
`library/skill-output-samples/**/sample_*.md` and cannot see `skills/*/references/EXAMPLE.md`.

**Disposition is a genuine decision**, recorded rather than settled: the defect is deeply
pre-existing and belongs to an already-deferred class, which argues for deferral; but it sits in the
flagship skill of this release, and the equivalent instance in `foundation-persona` was **fixed**
rather than deferred at round 5, which argues for consistency.

### R8-F4 (medium) - the fix over-corrected

> The 0.83 versus 0.81 observation is directional evidence, although the missing paired analysis
> means its uncertainty and materiality are unknown. Saying it is not evidence of an advantage makes
> another unsupported statistical conclusion in the opposite direction.

**CONFIRMED, and it is the sharpest self-correction of the cycle.** The round-7 fix replaced "sits
inside the noise" with "not evidence of an advantage and not evidence against one". The second half
is fine; the first half asserts a negative that the data does not support either. A 2-point gap **is**
directional evidence; what is unknown is whether it is reliable or material. The reviewer's own
wording is the correct landing point: *the point estimate does not establish a reliable or material
advantage.*

## Judged against the pre-registered standard (`e32a117d`)

| Clause | Result |
|---|---|
| 1. No P0 | Met |
| 2. P1 closed or deferred with rationale | R8-F1, F2, F4 close; **R8-F3 requires a maintainer decision** |
| 3. P2, P3 acknowledged | None returned |
| 4. No unsupported quantitative claim | R8-F3 and R8-F4 both engage it; F4 closes, F3 pending |
| 5. Fix-pass damage is P1 regardless of label | Engaged for R8-F1, F2, F4 |

**Maintainer attestation: NOT GIVEN, and not sought on this round.**
