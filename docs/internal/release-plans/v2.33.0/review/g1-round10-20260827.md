# G1 adversarial review, round 10 (2026-08-27)

**Verdict: needs-attention.** 5 findings (3 high, 2 medium). Trend: **5, 6, 8, 6, 6, 6, 4, 4, 5, 5.**
HEAD reviewed `a1648780`, thread `01a04516-8855-7711-a555-4e2bf83101dd`. Completion verified genuine.

## The pre-declared scope boundaries worked

Round 10 was the first round run with explicit scope boundaries on the record: that the
instrumentation template deliberately does not author test choreography, and that
`define-prioritization-framework` deliberately declines to define "clearly leads" numerically.

**Codex stayed inside both.** It did not re-raise "the template lacks test detail" or "the skill
needs a signal threshold". Instead it attacked what the fix actually claimed, and both high findings
against those surfaces landed squarely in the territory declared fair game: R10-F1 is a **policy-row**
failure, and R10-F2 is a **representational** failure. That is the pre-registration mechanism doing
its job, and it is the same mechanism that made the evaluation-sizing block converge after D15.

## Findings, all five from `a1648780` and all closed

| Finding | Sev | What it caught |
|---|---|---|
| R10-F1 | high | The replacement did not enforce what D22 decided: per-row normal-and-failure coverage was required only for the **minimization** rows, and the owner column accepted any team. Retention, sampling, opt-out and access could be marked tested on happy-path checks alone |
| R10-F2 | high | The new Kano contract demanded a per-feature response distribution for **every** run, but an inferred run works from interview themes and support patterns and cannot produce one. The contract required fabrication to satisfy it |
| R10-F3 | high | The interview sample's **appendix** repeated the two-researcher coding claim, plus compensation, an agreement rule and quote editing, all unmarked. The round-9 fix marked the Methodology sentence and not its sibling section |
| R10-F4 | medium | `four other features` in the PRD example is an observation, and the round-9 classifier was **digit-only**, so a spelled-out quantity was invisible to it |
| R10-F5 | medium | `HISTORY.md` still specified the two-sentinel choreography that D22 deleted from the template, so the canonical version history contradicted the shipped contract for the same version |

## Dispositions

- **R10-F1:** every policy row now needs normal-path **and** failure-or-degraded-path coverage, with
  the reason written in the table where failure injection is genuinely meaningless, and a named QA
  owner rather than a team alias. The rationale is spelled out for the non-minimization rows, since
  a retention job that never runs, a sampler stuck at zero, a stale role grant and an opt-out that is
  recorded but not honoured are all invisible to a happy-path check.
- **R10-F2:** the distribution column is required for **surveyed** runs and reads
  `not available (inferred)` otherwise, with the qualitative signal and its source base carried
  instead. A fabricated spread would be worse than a missing one because it would make an inferred
  call look surveyed.
- **R10-F3, R10-F4:** marked, and the sweep method was corrected. The round-9 enumeration matched
  digits only; it now covers spelled-out quantities, which is how `four other features` survived.
- **R10-F5:** the HISTORY narrative now describes the shipped contract, and records why the
  choreography was deleted rather than corrected again.

**One sibling closed without being asked.** D19's own record described the test assertion it added,
which D22 later deleted. It now carries a partial-supersession note stating that D19's principle was
not overturned but extended: D19 said a PM template names the contract rather than enumerating queue
mechanics, and D22 applied that reasoning to the test steps D19 had left standing.

## Judged against the pre-registered standard (`e32a117d`)

No P0. All five closed rather than deferred; clause 5 applied since all five were fix-pass damage.
No pre-existing finding surfaced this round, the first round of the gate where that is true.

**Maintainer attestation: NOT GIVEN, and not sought on this round.**
