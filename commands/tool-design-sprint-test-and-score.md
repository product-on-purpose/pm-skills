---
description: Day 5 (Friday) of a Design Sprint - run 5 customer interviews, score sprint questions, capture hot takes, and produce the Decider's build / iterate / pivot / stop call
---

Use the `tool-design-sprint-test-and-score` skill to produce Friday's sprint-closing artifact.

Read the skill instructions from `skills/tool-design-sprint-test-and-score/SKILL.md` and follow them to capture per-customer interview observation notes, surface best quotes, build the scorecard grid (sprint-question rows by 5 customer columns; day-end decision per row using Validated/Invalidated/Inconclusive rules), name observed patterns in 4 buckets (worked, hesitated, broke trust, unexpected), collect hot takes from each team member (written silently in parallel BEFORE group synthesis), and capture the Decider summary (build / iterate / pivot / stop / reframe + highest-confidence learning + most important revision + next artifact).

Use `skills/tool-design-sprint-test-and-score/references/TEMPLATE.md` as the output format.

Context from user: $ARGUMENTS
