# Codex Agent Context

> **Currency marker: v2.25.1 (SHIPPED 2026-06-06; tag `v2.25.1` at `2b5044a`; GitHub Release Latest; `agent-plugins` re-pinned metadata 1.9.0).** Required for `check-context-currency` CI; updated at each release-tag time alongside `_agent-context/claude/CONTEXT.md`. v2.25.1 banks accumulated untagged work since v2.25.0: Pattern S docs-site reorg + family-standard conformance, a generated CI-gated resource index, an em-dash-scar cleanup with new advisory + enforcing CI guards, three site dependency bumps, and a pre-tag validator parity fix; no new skills (catalog stays 65). Previous: v2.25.0 (SHIPPED 2026-06-03; tag `v2.25.0` at `23e65da`; GitHub Release Latest; squash-merged PR #161; site deployed) is the activation and trust layer: the plugin's first hooks (F-43 opt-in `PreToolUse` house-rule guardrails gated by `.claude/pm-skills.local.md`, blocking em-dash/en-dash and warning on placeholders/metrics; F-44 confident-only `SessionStart` phase router reading `phase:` from SKILL.md frontmatter) plus M-30, an advisory output-quality eval tier (3 deterministic invariant validators over recorded samples); dependency-free Node hooks, fail-open; no new skills (catalog stays 65). Previous: v2.24.0 SHIPPED 2026-06-01 (tag `d3f1549` on main) adds the `pm-workflow-orchestrator` sub-agent + its `utility-pm-workflow-orchestrator` dispatch skill (a governed multi-skill runner that takes a saved `foundation-prioritized-action-plan` plan, or a user-named chain, and runs an ordered sequence of pm-skills with per-step go/no-go checkpoints; CHECKPOINTED default + GUARDED-AUTO opt-in; the engine delegates via the `Skill` tool, not `Agent`, so it adds zero chain depth and needs no `_chain-permitted` entry; ships EXPERIMENTAL on non-Claude clients) and grows `foundation-prioritized-action-plan` to v1.1.0 with an optional `--run` handoff into the orchestrator; catalog 64 to 65 (utility 10 to 11). Previous: v2.23.0 SHIPPED 2026-05-31 (tag `v2.23.0` at `b54cef0`, GitHub Release Latest, `agent-plugins` re-pinned `5f5a3c0`, registry metadata 1.0.2, all post-tag CI green) adds one foundation skill, `foundation-prioritized-action-plan` (evidence-grounded prioritized action plans via Theory of Constraints + Cynefin); catalog 63 to 64 (foundation 8 to 9); additive minor, no existing skill changed. No command wrapper (post-v2.22.0 convention) and no HISTORY.md at launch (created on first iteration). Release hygiene done (counts re-derived, manifests + README badge to 2.23.0, CHANGELOG + Release_v2.23.0.md), also added the `check-skill-sample-coverage` enforcing CI gate (W2: phase/foundation/tool skills must ship 3 thread samples); Codex adversarial review applied pre-merge; squash-merged PR #150, tagged, released, registry re-pinned; all post-tag CI green on Linux + Windows. v2.22.0 SHIPPED 2026-05-30 (tag `v2.22.0`) is the wrapper-deletion release: the 63 per-skill command wrappers were deleted (the 10 workflow-* commands remain), all 63 skill names unchanged, master-plan D6 retired (agents/_pairing.yaml deleted), and a Codex .codex-plugin/plugin.json manifest added; pre-tag gates green, awaiting tag. v2.21.0 is the additive marketplace launch: pm-skills is published through the now-public `product-on-purpose/agent-plugins` registry (pinned via an https `url` source, metadata 1.0.0) while the self-hosted path keeps working (dual-home); no skill changes (catalog 63); a minor (nothing removed; the old-path retirement is reserved for a future major). Tag `v2.21.0` -> `1065c3e`, GitHub Release Latest; smoke caught + fixed two install-path SSH/source bugs before the public flip. v2.20.0 adds 3 sprint `/workflow-` commands (foundation-sprint, design-sprint, foundation-to-design; slash commands 70 -> 73) plus validator/doc hardening (command-sync requires a file per workflow row; count-consistency number-after + singular-noun coverage; vestigial check-stale-bundle-refs removed); no new skills, catalog stays 63; annotated tag `v2.20.0` -> `e1db5ec`, GitHub Release Latest, tag CI green. v2.19.0 is pre-promotion hardening (validator/CI/script hardening + promotion-readiness; no new skills, catalog stays 63; the pre-tag bundle now runs 18 enforcing validators with bash + pwsh parity); annotated tag `v2.19.0` -> `a18e4d5`, GitHub Release Latest, tag CI green. v2.18.0 adds four highest-consensus content skills (discover-market-sizing, define-prioritization-framework, discover-journey-map, measure-survey-analysis); catalog 59 to 63 (phase 26 to 30); annotated tag `daf720e`, GitHub Release Latest, tag CI green. v2.17.0 SHIPPED 2026-05-20 (annotated tag at `b6ff373`; GitHub Release published; tag CI green): native Claude Code sub-agent registration (`AGENTS/` to `_agent-context/`, `subagents/` to `agents/`) + frontmatter metadata-nested migration + bash-3.2 validator portability + check-version-references advisory wiring. v2.16.2 SHIPPED 2026-05-19 (post-v2.16.1 audit hygiene fast-patch closing F-P1-02 CONTEXT.md Status refresh + F-P2-01-half check-context-currency.sh wire into pre-tag-validate). v2.16.1 SHIPPED 2026-05-19 (plugin manifest schema patch removing invalid `agents` field; G4 P0 FULL PASS cross-platform attestation). v2.16.0 SHIPPED 2026-05-18T15:05:08Z (tag points at `b4d5172`; GitHub Release at https://github.com/product-on-purpose/pm-skills/releases/tag/v2.16.0). Previous markers: v2.12.0 (2026-05-03; never updated through v2.13.0 / v2.14.0 / v2.15.x cycles) -> v2.16.0 -> v2.16.1 -> v2.16.2.

## Status

This file is intentionally minimal as of 2026-05-05 (v2.13.0 cycle, Bucket B.5). The minimal posture persisted through v2.14.x and v2.15.x; v2.16.0 retains this posture.

Codex usage in this repo is now scoped to **Phase 0 adversarial review** (the cross-LLM review loop codified in v2.11.0 and operated via the `codex:rescue` plugin in current cycles; the original `/jp-ai-review` skill is deprecated). Codex executed 4 adversarial review passes during v2.16.0 (plan-state review, ship-state review at 9cb81af, pre-tag defect pass at 19a213b, challenge review at 19a213b). Plus Codex CLI 0.128.0 self-administered the GATE B + GATE C cross-client harness at `docs/internal/release-plans/v2.16.0/maintainer-gate-testing-codex.md` to validate the dispatch skill mechanism. Codex is not used for primary execution work, so this file does not maintain a per-session execution snapshot.

For all current pm-skills state - active work, recent commits, decisions, validator inventory, release status - read **`_agent-context/claude/CONTEXT.md`** instead. That file is the canonical project context across both agents.

## When to update this file

Three triggers, in priority order:

1. **Codex execution scope change.** If Codex resumes primary execution work in this repo (beyond adversarial review), restore the historical structure: per-session execution snapshot + immediate next steps + reference links.
2. **Phase 0 review protocol change.** If the `/jp-ai-review` skill or the cross-LLM review loop pattern materially evolves, document the new contract here.
3. **Release tag time.** Bump the currency marker above to match the just-tagged CHANGELOG version. The marker is what `check-context-currency` validates.

## Reference

- Canonical project context: `_agent-context/claude/CONTEXT.md`
- Cross-agent decisions: `_agent-context/DECISIONS.md`
- Cross-LLM review protocol: `docs/internal/cross-llm-review-protocol.md`
- Phase 0 Adversarial Review Loop template: `docs/internal/release-plans/v2.11.0/plan_v2.11_pre-release-checklist.md`
- Codex local technical decisions (if any): `_agent-context/codex/DECISIONS.md`
- Session logs (shared, model encoded in filename): `_agent-context/SESSION-LOG/` (legacy: `_agent-context/codex/SESSION-LOG/`)

## History

This file was actively maintained 2026-01 through 2026-03-22 (v2.6 - v2.7.0 era) when Codex shared primary-execution duties with Claude. It carried per-session execution snapshots, immediate next steps, and detailed handoff notes during that period. Active maintenance was deprioritized when Codex's role narrowed to Phase 0 adversarial review (v2.11.0). The file received currency-marker bumps only through v2.12.0. The 2026-05-05 vestigial-redirect rewrite (Bucket B.5 of v2.13.0) made the deprioritization explicit and removed stale execution content. Git history (`git log -- _agent-context/codex/CONTEXT.md`) preserves the full prior content if needed.
